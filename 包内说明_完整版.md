# ConvTasNet-Spatial · 可交付模型包（自描述 · 可独立加载）

> **一句话**：拿过去就能跑的**远场双说话人分离**模型包。输入一段**带 3 个麦克风的录音**，
> 输出**两路**语音。包里没有"训练才需要的东西"，只有"跑模型需要的东西"。

---

## 0. 它和"官网下载的模型"是什么关系

官网（HuggingFace `JorisCos/ConvTasNet_Libri2Mix_sepclean_16k`）下载下来的东西是两件：

```
config.json         ← 配方：网络怎么搭（层数、通道数、卷积核、滤波器组类型…）
pytorch_model.bin   ← 权重：345 个张量
```

**本包的目标模型就是从那两件起步的**：以那份官方权重为起点，在自建仿真远场训练集
（5777 条 / 20.46 h）上微调 30 轮，并把 mask 网络的 bottleneck 输入通道从 **512 扩到 518**，
用于接入 6 路空间特征。其余结构（编码器 / TCN / 解码器 / mask 头）一字未改。

所以本包采用**同样的形态**：

| 官方下载 | 本包 |
|---|---|
| `config.json`（配方） | `config.json`（配方，另加空间特征契约与训练溯源） |
| `pytorch_model.bin` | `convtasnet_spatial.pt`（纯权重，345 张量 / 5 067 697 参数） |
| 建模代码在 `asteroid` 库里 | 建模代码随包发出（`conv_tasnet_spatial.py`，纯 PyTorch） |

**一个区别**：官方那份必须装 `asteroid` 才能 `from_pretrained` 加载；本包把建模代码一起发出来，
**只需要 `torch` + `numpy`** 就能加载，离线可用、不受第三方框架版本变化影响。

---

## 1. 文件清单

| 文件 | 作用 |
|---|---|
| `0_三步跑通.md` | **评委先看这份**：一页三步跑通（装依赖 / 双自检 / 跑一条自带样本） |
| `separate.py` | **主脚本**，你唯一需要跑的入口 |
| `spatial_feat.py` | 空间特征提取器（6 通道 ITD/ILD），可单独跑自校验 |
| `conv_tasnet_spatial.py` | 网络定义（纯 PyTorch），相当于官方的"建模代码" |
| `model_loader.py` | 加载与推理封装（严格加载 + 帧对齐） |
| `config.json` | **结构配方**：网络参数 + 空间特征契约 + 训练溯源 |
| `convtasnet_spatial.pt` | **权重**（20 389 069 B / md5 `c04bfb9257351ecb7b95bef369bfec6a`） |
| `space_feat_stats.json` | 6 通道标准化标尺（mean/std） |
| `load_and_infer.py` | 最小示例：50 行看懂"怎么加载、怎么喂" |
| `素材/` | 6 条现成的 7 通道仿真样本，开箱即跑 |
| `训练记录/` | 留档（非交付面）：`ckpt_ep30.pt` + 训练侧代码 + 随包官方底座 + 模型说明 |
| `README.md` | 本文档 |

```
ConvTasNet-Spatial/
├─ 0_三步跑通.md           ← 评委先看这份（三步跑通）
├─ separate.py             ← 跑这个
├─ spatial_feat.py
├─ conv_tasnet_spatial.py
├─ model_loader.py
├─ load_and_infer.py
├─ config.json
├─ convtasnet_spatial.pt
├─ space_feat_stats.json
├─ 素材/
│  ├─ main_meeting_pos_d8m_1580-141083-0053.wav   7 通道 16 kHz
│  ├─ ...（共 6 条）
│  └─ 素材清单.csv
├─ README.md
└─ 训练记录/              ← 留档：ckpt_ep30.pt + 训练侧代码 + 官方底座 + 模型说明
```

调用关系：

```
separate.py
 ├─ spatial_feat.py         ← 算 6 通道空间特征
 ├─ space_feat_stats.json   ← 做 z-score
 ├─ model_loader.py         ← 加载权重 + 推理
 │   └─ conv_tasnet_spatial.py  ← 按 config.json 搭网络
 ├─ config.json
 └─ convtasnet_spatial.pt
```

---

## 2. 怎么装、怎么跑

### 依赖

| 包 | 必需？ | 说明 |
|---|---|---|
| `torch` | **必需** | >= 1.13；CPU 版即可，有显卡会自动用 |
| `numpy` | **必需** | — |
| `soundfile` | 建议 | 读写 wav 更快；没装会自动退回 `scipy.io.wavfile` |
| `scipy` | 建议 | 没有 `soundfile` 时用它读写 wav，以及非 16 kHz 输入的重采样 |
| `matplotlib` | 可选 | 画逐帧特征图；没装就只出 CSV |

不需要 `asteroid`，不需要联网，不需要官方 `pytorch_model.bin`。

### 跑

```bash
# 最基本（用包里自带的素材）
python separate.py 素材/main_meeting_pos_d8m_1580-141083-0053.wav

# 指定输出目录 / 强制 CPU
python separate.py 我的三麦录音.wav --outdir 结果 --device cpu

# 长录音：按 15 秒分块、块间重叠 1 秒
python separate.py 长录音.wav --segment-seconds 15 --overlap-seconds 1
```

### 参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `input` | — | 输入音频，**必须 >= 3 通道** |
| `--outdir` | 与输入同目录 | 输出目录 |
| `--config` | 包内 `config.json` | 结构配方（一般不用改） |
| `--ckpt` | 包内 `convtasnet_spatial.pt` | 权重文件 |
| `--scaler` | 包内 `space_feat_stats.json` | 标准化标尺。**换一份就等于换一套输入尺度**，不要随手替换 |
| `--device` | 有显卡用 `cuda` | `cpu` / `cuda` |
| `--ref-channel` | `1` | 参考麦通道号（linear3 的中心麦） |
| `--segment-seconds` | `20` | 分块长度；`0` = 不切块 |
| `--overlap-seconds` | `0.5` | 块间重叠，用于交叉淡化接缝 |

### 跑起来大概多快

一条 4.04 秒的 7 通道样本（含读盘、特征提取、推理、写盘的全部耗时）：

| 设备 | 用时 |
|---|---|
| CPU（本机 x86，torch 2.11） | 3.1 s |
| GPU（本机 CUDA） | 2.5 s |

### 自查（不用任何数据）

```bash
python spatial_feat.py --selftest    # 特征提取器：合成信号核验 ITD/ILD 精度、符号、形状契约
python conv_tasnet_spatial.py --check # 网络：按 config.json 建网 + 载权重 + 前向 + 三道闸
```

两个都应当打印 `PASS`。

---

## 3. 输入要求（错了不会报错，只会静默变差）

| 项 | 取值 |
|---|---|
| 通道数 | **>= 3**（第 4 路起忽略；7 通道样本可直接用前 3 路） |
| 前 3 路是什么 | **linear3 线性三麦**：ch0 / ch1 / ch2，间距 **2 cm** |
| 参考麦 | **ch1**（波形只取这一路；这点和"对多通道求平均"完全不同） |
| 采样率 | **16 kHz**（其它采样率会自动重采样，需要 `scipy`） |
| 几何 | 阵列方向写死为 y 轴 −2 / 0 / +2 cm |

> ⚠️ 阵列几何、通道顺序、间距这三条是**写死**的。摆错了不会报任何错，只会让空间特征
> 跑到训练分布之外、结果变差 —— 这是本模型最容易被误用的地方。

---

## 4. 输出是什么

```bash
<输入名>_spk1.wav      16 kHz / PCM16 / 峰值归一 0.9
<输入名>_spk2.wav      同上
<输入名>_debug.png     逐帧 ITD/ILD 曲线 + 几何上限参考线（没装 matplotlib 时退化为 .csv）
<输入名>_debug.csv     逐帧空间特征（原始量纲 + z 版），任何时候都会写
```

> ⚠️ **输出是"两路"，不是"目标那一路"。** 模型是双说话人分离器，`_spk1` / `_spk2` 的编号
> 是任意的，**先听再选**。要"只留某一个人的声音"需要再做一步声纹比对（不在本包内）。

运行时打印四步进度：

```
[1/4] 设备：cuda（torch 2.11.0+cu128，io=soundfile）
[2/4] 模型就绪：5067697 参数（5.0677 M），权重来自 convtasnet_spatial.pt（训练第 30 轮）
[3/4] 读到 素材.wav：7 通道 / 16000 Hz / 64613 样本（4.04 秒）-> 取 ch1 为波形，ch0-2 算空间特征
      空间特征质检：夹取前越界帧 [0, 240, 0]（占 1.9812%），触及搜索窗边界（估计器失效）[0, 118, 0]（占 0.9741%）
[4/4] 分离完成：输出 2 路，每路 64613 点，用时 2.5 s
```

第 3 步那行"质检"是**给使用者看的体检报告**：ITD 是有限量（2 cm 阵列最多 58 微秒），
超过几何上限的帧一定是相关峰选错了。`itd_02`（4 cm 长基线）的越界比例天然比另两对高一个量级，
这是远场混响下的正常现象，不是崩溃信号；但比例如果到几十个百分点，就该怀疑阵列几何或通道顺序配错了。

---

## 5. 它做到了什么（句级口径）

**评测条件**：仿真远场测试网格 10800 条（3 房间 × 6 距离 × 600 条），统计单元 = 句子，
每个极性 **n = 300 句**；目标恒 az+0°、干扰恒 ±90°、SIR 恒 0 dB。

| 指标 | 值 |
|---|---|
| SI-SDR（选路后） | **6.3433 dB** |
| SI-SDRi（选路后） | **6.3441 dB**（95% CI [6.0652, 6.6266]） |
| WER（TSE 后） | **0.3377**（混合直连 0.5809；相对降 41.9%） |
| 与单通道基线（同数据、同轮数微调）的差 | ΔSI-SDRi **+0.0156 dB**（95% CI [−0.0118, +0.0447]，句级 **p = 0.62**） |
| ΔWER（同单通道基线） | **−0.0012**（句级 p = 0.68） |

**如实说明**：

- 增益**很小**（+0.0156 dB），**句级统计不显著**；
- ±45° 干扰方位下 +0.0173 dB（p = 0.39）—— 只能写"**未观察到崩塌**"，不能写"有提升"；
- `office` 房间 +0.0086 dB（p = 0.51，未观察到受益），`meeting` +0.0079（p = 0.88），`hall` +0.0303（p = 0.57）；
- 10 m 档结果不稳（正负向都出现过波动）；
- **不具备拒识能力**：目标人不在场时它会把在场的另一个人完整输出
  （负向样本 SI-SDRi 约 −59 dB）。

干净数据上的上界（另一组对照条件）：微调 30 轮在仿真域内带来 **+3.3297 dB**，
其余增益来自基础模型的域外泛化；这两件事不是同一把尺子，不要混着引用。

---

## 6. 必须声明的边界（不能省）

1. **标尺就是训练时那份** —— `space_feat_stats.json` 是训练侧 train-fit 统计量
   （5777 条 / 73 663 859 帧，md5 `f0041eb724322535e0016b896722d2ff`），
   本包用它做 z-score 时**逐位复现**了训练时落盘的特征（实测 5 条样本 max|Δ| = 0）。
   所以本包跑出来的数字与 README 里引用的数字**是同一批**。换掉这份标尺就等于换一套输入尺度。
2. **上面那个 +0.0156 dB 是在特定网格下测的** —— 目标恒 az+0°、干扰恒 ±90°、SIR 恒 0 dB。
   换一个几何，表现**可能明显不同**（也可能更好，因为踩不到那条分布偏移）。
3. **仿真是仿真** —— 仿真结果不等于真实远场录音上的表现。
4. **中文未经验证** —— 训练语料是英文（LibriSpeech 干声 + 自建 RIR 仿真）。中文效果需另行测试。
5. **不具备拒识能力** —— 见上。
6. **不是声纹条件 TSE** —— 它不认识"谁"，只负责把人分开，不负责挑出指定的人。
7. **不是流式** —— 整段（或分块）处理，有秒级延迟。
8. **训练数据与生成脚本不在本包内** —— 本包只保证"能加载、能跑、口径对"；
   要复现训练需要本仓库 `训练记录/` 里的训练侧代码与检查点。**权重不可再生成，请备份 `convtasnet_spatial.pt`。**

---

## 7. 一致性核验：为什么可以信这个包

本包的建模代码是自己写的（纯 PyTorch），所以必须证明它和训练时用的实现**完全一致**。
核验方式：把**同一份权重**分别灌进「训练侧实现（asteroid 0.7.0）」和「本包实现」，喂同样输入，
逐元素比对输出：

| 用例 | max|Δ| |
|---|---|
| 随机波形 T = 16000（1 s） | **0.000e+00** |
| 随机波形 T = 48000（3 s） | **0.000e+00** |
| 真实仿真样本 T = 156066（9.75 s） | **0.000e+00** |
| 同上，GPU | **0.000e+00** |

另有两项静态核验：权重张量名集合完全一致（345 / 345）、参数量完全一致（5 067 697）。
即：**本包换掉的是依赖，不是模型。**

唯一的版本差异来自 `numpy` 版本不同时的浮点累加顺序，实测影响量级：
z 域特征 max|Δ| = 4.6e-05，两路输出之间的 SI-SDR = 131 dB（听不出来、也测不出来）。

---

## 8. 与官方原版、与单通道基线的关系

| | 官方 Conv-TasNet | **本模型** | 单通道基线 |
|---|---|---|---|
| 输入 | 单通道波形 | 单通道波形（ch1）**+ 6 路 ITD/ILD** | 单通道波形 |
| bottleneck 输入通道 | 512 | **518** | 512 |
| 参数量 | 5 066 929 | **5 067 697**（+768） | 5 066 929 |
| 需要什么硬件 | 任意麦克风 | **2 cm 间距线性三麦** | 任意麦克风 |
| 能否独立使用 | — | 需要 3 通道输入 + 特征提取器 + 标尺（本包已配齐） | 需要权重与配方 |

> 说明：本模型相对官方底座**唯一的结构改动**就是 bottleneck 那一层的输入通道数，其余完全相同 ——
> 这是"早融合空间特征"这个自变量能被干净地归因的前提。