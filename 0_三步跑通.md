# 三步跑通（Conv-TasNet-Spatial · 快速上手）

> **简介**：装 `torch` + `numpy` → 自检打 `PASS` → 跑一条自带样本出两路语音。
> **不需要** `asteroid`、**不需要**联网、**不需要** GPU、**也不需要**官方 `pytorch_model.bin`。

前置：一个能 `pip install` 的 Python 环境；以下命令都在**本包根目录**执行。
本机已实测跑通：Windows Python 3.12（torch 2.13，没装 `soundfile` 时自动走 `scipy` 回退）
与 Linux + CUDA（torch 2.11 + cu128）。

**不同平台看这里**：

| 平台 | 要注意 |
|---|---|
| Windows | 直接 `pip install torch` 会拉 **2.5 GB 的 CUDA 版**；只用 CPU 就照第 1 步里那条 CPU 版命令装，省 2.3 GB |
| Windows 一键版 | 不想手敲：双击 `自检.bat`，它等价于第 1、2 步再加跑一条样本 |
| Linux / macOS | 很多系统只有 `python3`，把下面所有 `python` 换成 `python3` |
| 输出重定向到文件 | 先 `set PYTHONIOENCODING=utf-8`（Windows）或 `export PYTHONIOENCODING=utf-8`，否则中文可能乱码；`PASS` / `OK` 这类判断不受影响 |

---

## 第 1 步 · 装依赖（约 1 分钟）

```bash
pip install torch numpy          # 必需；CPU 版即可，有显卡会自动用
pip install soundfile scipy      # 建议；wav 读写更快、非 16 kHz 输入可自动重采样
```

**Windows / 只装 CPU 版**（少下 2.3 GB，推荐先试这条）：

```bash
pip install torch --index-url https://download.pytorch.org/whl/cpu
pip install numpy soundfile scipy
```

`matplotlib` 可选（画逐帧特征图；没装就退化成 CSV）。装完**不需要再下载任何模型文件**。

---

## 第 2 步 · 自检（约 1 分钟，不用任何音频）

```bash
python conv_tasnet_spatial.py --check     # 按 config.json 建网 + 载权重 + 前向 + 三道闸
python spatial_feat.py --selftest         # 特征提取器：合成信号核验 ITD/ILD 精度与符号
```

两条都应打印 `PASS`。第一条成功即证明三件事**配套**：

- 参数量 **5 067 697**（5.0677 M）、张量 **345** 个 —— 与 `config.json` 里的 `num_params` 一致；
- `config.json`（配方）与 `convtasnet_spatial.pt`（权重）**同源**，缺任何一件都建不起来；
- 帧数闸与通道数闸有效：空间特征给错帧长、或只给 2 通道，会被**直接拒绝**，不会静默跑偏。

---

## 第 3 步 · 跑一条自带样本（约 3 分钟）

```bash
python separate.py 素材/main_meeting_pos_d8m_1580-141083-0053.wav --outdir 结果
```

输出 4 件：

| 文件 | 内容 |
|---|---|
| `结果/xxx_spk1.wav` | 分离出的第 1 路，16 kHz / PCM16 / 峰值归一 0.9 |
| `结果/xxx_spk2.wav` | 第 2 路，同上 |
| `结果/xxx_debug.png` | 逐帧 ITD / ILD 曲线 + 几何上限参考线（没装 `matplotlib` 时退化为 `.csv`） |
| `结果/xxx_debug.csv` | 逐帧空间特征（原始量纲 + z 版） |

> **先听再选**：`spk1` / `spk2` 的编号是任意的 —— 模型负责"把人分开"，不负责"挑出指定的人"。

实测耗时（4.04 秒 / 7 通道样本，含读盘与写盘）：**CPU 约 3 秒（3.1–3.5 s）、GPU 约 2.5 秒**。
换成自己的录音就是**同一条命令换个文件名**，但要先满足下面三条。

### 怎么算跑通了

| 步骤 | 成功的样子 |
|---|---|
| 第 1 步 | `pip` 最后一行是 `Successfully installed …`，没有 `ERROR` |
| 第 2 步 | 两条命令各打印一次 **PASS** |
| 第 3 步 | 末尾出现 `[4/4] 分离完成：输出 2 路…`，且 `结果\` 下多出 4 个文件 |

`--outdir` 指定的目录不存在也没关系，脚本会自己建。

---

## 换成自己的录音：三条硬要求

| 项 | 要求 |
|---|---|
| 通道数 | **≥ 3**（第 4 路起忽略；7 通道样本可直接用前 3 路） |
| 前 3 路 | **2 cm 间距的线性三麦** `ch0` / `ch1` / `ch2`；**`ch1` 是参考麦**（波形只取这一路，不是多通道平均） |
| 采样率 | **16 kHz**（其它采样率会自动重采样，需要 `scipy`） |

阵列几何**写死**为沿 y 轴 −2 / 0 / +2 cm。摆错位置、接错通道顺序**都不会报错**，只会让结果静默变差。

---

## 三个已踩过的坑

1. 用 `model(wav)` **前向**，不要用 `model.separate()` —— 官方权重里的 `sample_rate` 被写成 8000，文件接口会静默重采样。
2. **别去开 TF32**：`conv_tasnet_spatial.py` 导入时已默认关闭（开了会让 512 与 518 通道走不同精度）。
3. **别换 `space_feat_stats.json`**：它是训练时那份标尺（md5 `f0041eb7…`），换一份就等于换一套输入尺度。

---

## 它是什么 / 不是什么

- **是**：远场**双说话人分离**模型（Conv-TasNet 底座 + 6 路 ITD/ILD 空间特征早融合，微调 30 轮）。
- **不是**：声纹条件 TSE、**不具备拒识**（目标人不在场时会完整输出在场的另一个人）、不是流式、中文未经验证。

成绩数字与 8 条边界声明见 `README.md`（简版）与 `包内说明_完整版.md` §5–§6（完整版）；
结构参数、本模型相对官方底座做的改动、微调口径见 `训练记录/模型说明.md`。

---

包根文件：`README.md`（仓库首页）· `包内说明_完整版.md`（完整说明）· `自检.bat`（Windows 一键自检）·
`config.json`（配方）· `convtasnet_spatial.pt`（权重，345 张量 / 20 389 069 B）·
`space_feat_stats.json`（标尺）· `separate.py` / `spatial_feat.py` / `conv_tasnet_spatial.py` / `model_loader.py` /
`load_and_infer.py`（代码）· `素材/`（6 条 7 通道样本 + 清单）· `训练记录/`（训练 ckpt + 训练侧代码 + 官方底座 + 模型说明）
